/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add common settings if needed
  images: {
    domains: ['avatars.steamstatic.com', 'faceit-avatars.s3.amazonaws.com'],
  },
};

module.exports = nextConfig;
